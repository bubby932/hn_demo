#pragma once

#include <stddef.h>

namespace must {
	class VirtualAllocator {

		public:
		void *VirtualAllocator::allocate(size_t size) {
			
		}
		void VirtualAllocator::deallocate(void *ptr) {

		}
	};

}